import React from 'react';
import { Text } from 'react-native';

import { Container } from './styles';

const Repo: React.FC = () => {
  return (
    <Container>
      <Text>Repo</Text>
    </Container>
  );
};

export default Repo;
